---
title: Code includes fixture
---

This document has a code block with `inc` in the info string. The block content
is a file path; the filter replaces it with that file's contents.

``` inc
tests/fixtures/included.txt
```

The code block above should be replaced by the contents of that file.
